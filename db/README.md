# db/

`schema/` – Drizzle-Tabellendefinitionen, ein File pro Domaene (siehe ARCHITECTURE.md Abschnitt 4).
`migrations/` – generierte Drizzle-Migrationen.
`client.ts` – Neon-Verbindung (folgt in M2).
