# components/

Reine, wiederverwendbare UI-Komponenten (shadcn/ui-basiert).
Keine Businesslogik, keine Server Actions, keine direkten DB-/API-Aufrufe.
Feature-spezifische Komponenten leben in `features/<name>/components`.
